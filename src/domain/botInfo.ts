/**
 * The bot's public Telegram identity. Shared by the landing page and any
 * user-facing message that needs to link back to the bot (e.g. the
 * `/sharetext` invite message), so the handle and URL are defined once
 * instead of being duplicated as string literals across the codebase.
 */
export const BOT_USERNAME = "@ifs_relay_bot";
export const BOT_URL = "https://t.me/ifs_relay_bot";
